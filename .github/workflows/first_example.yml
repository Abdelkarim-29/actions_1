name: my first workflow 

on: push 
jobs: 
    first_jop:
        runs-on: ubuntu-latest
        steps:
        - name: checkout Repo
          uses: actions/checkout@v5  
        - name: welcome
          run: echo "abdelkarim"
        - name: list files
          run: ls
        - name: read file
          run : cat README.md   

