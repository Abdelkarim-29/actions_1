name: my first workflow 

on: push 
jobs: 
    first_jop:
        runs-on: ubuntu-latest
        steps:
        - name: checkout Repo
          uses: actions/checkout@v5  
        - name: welcome
          run: |
            echo "abdelkarim"
            ls -ltra
            cat README.md
        - name: genrate ASCI 
          run: |
            sudo apt install cowsay
            cowsay -f dragon tux "I am Linux" >> dragon.txt
    

