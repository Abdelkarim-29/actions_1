name: My First Workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Shell Script
        run: |
          ls -la
          chmod +x script.sh
          ./script.sh
 
